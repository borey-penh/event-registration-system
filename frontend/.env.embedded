# Env for the EMBEDDED build (bash scripts/build-embedded-frontend.sh).
# The SPA is served by Laravel itself, so the API is the same origin the page
# is opened from — see src/lib/api.js. Keep VITE_API_URL unset here.
VITE_PUBLIC_ORIGIN=embedded
